---
import Input from "@/components/shared/Input.astro";
// import ImagesDropzone from "@/components/dashboard/ImageDropzone.astro";
import { Icon } from "astro-icon/components";
import Button from "@/components/shared/Button.astro";

type FormBrand = {
  id?: number;
  slug?: string;
  name?: string;
  // image?: string | null;
};
interface Props {
  brand?: FormBrand;
}
const { brand = {} } = Astro.props as Props;
const creating = !brand.slug;
// const image = category.image ?? "";
---

<section class="pt-xl">
  <form
    novalidate
    id="brand-form"
    method="POST"
    data-mode={creating ? "create" : "edit"}>
    {!creating && <input type="hidden" name="id" value={brand.id} />}
    <h1 class="text-lg pb-2">
      {creating ? "Создание категории" : "Редактирование категории"}
      {
        !creating && brand.slug && (
          <Button
            size="sm"
            withIcon
            class="inline-block align-baseline ml-xs"
            href={`/store/${brand.slug}`}>
            <Icon name="eye" title="Открыть в новой вкладке" class="size-5" />В
            магазине
          </Button>
        )
      }
    </h1>

    <!-- <ImagesDropzone images={images} /> -->

    <div class="textInputFields">
      <div class="nameSlug">
        <Input
          name="name"
          label="Название"
          value={brand.name}
          required
          autofocus
          autocomplete="off"
        />
        <Input
          name="slug"
          label="Ярлык"
          value={brand.slug ? brand.slug : ""}
          readonly={true}
          required
        />
      </div>
    </div>
    <Button id="submit-btn" size="sm" type="submit">
      <span class="submit-text">{creating ? "Добавить" : "Сохранить"}</span>
      <Icon name="spinner" class="submit-spinner size-5 animate-spin" />
    </Button>
    {
      !creating && (
        <Button
          id="delete-button"
          size="sm"
          variant="destructive"
          type="button">
          Удалить
        </Button>
      )
    }
  </form>
</section>

<style>
  form {
    margin: auto;
    padding-bottom: 4rem;
    container-type: inline-size;
    max-width: 80ch;
  }
  .textInputFields {
    display: grid;
    gap: 0.7rem;
    container-type: inline-size;
  }
  .nameSlug {
    display: grid;
    gap: 0.7rem;
    grid-template-columns: 1fr;
    @container (min-width: 60ch) {
      grid-template-columns: 1fr 1fr;
    }
  }
</style>

<script>
  import { actions } from "astro:actions";
  import { navigate } from "astro:transitions/client";
  import { generateSlug } from "@/lib/slugGeneration";

  const brandForm = document.querySelector<HTMLFormElement>(
    "#brand-form",
  ) as HTMLFormElement;

  const mode = brandForm.dataset.mode;

  // Auto-generate slug from name
  const nameInput = brandForm.querySelector<HTMLInputElement>('[name="name"]');
  const slugInput = brandForm.querySelector<HTMLInputElement>('[name="slug"]');
  if (nameInput && slugInput) {
    nameInput.addEventListener("input", () => {
      slugInput.value = generateSlug(nameInput.value);
    });
  }

  //event listeners
  brandForm.addEventListener("submit", async (e) => {
    e.preventDefault();
    const formData = new FormData(brandForm) as FormData;
    if (brandForm.hasAttribute("disabled")) {
      return;
    }

    try {
      brandForm.setAttribute("disabled", "true");
      const { data, error } =
        mode === "edit"
          ? await actions.brand.updateBrand(formData)
          : await actions.brand.createBrand(formData);
      if (error) {
        throw new Error("Unable to submit the form");
      } else {
        navigate("/dashboard/brands");
      }
    } catch (error) {
      console.log(error);
      brandForm.removeAttribute("disabled");
    }
  });

  const deleteButton = document.querySelector<HTMLButtonElement>(
    "#delete-button",
  ) as HTMLButtonElement;
  if (deleteButton) {
    deleteButton.addEventListener("click", async () => {
      const formData = new FormData(brandForm) as FormData;
      const name = formData.get("name") as string;
      if (!confirm(`Удалить категорию  ${name}?`)) {
        return;
      }

      try {
        brandForm.setAttribute("disabled", "true");
        const { data, error } = await actions.brand.deleteBrand(formData);
        if (error) {
          throw new Error("Произошла ошибка при удалении");
        } else {
          navigate("/dashboard/brands");
        }
      } catch (error) {
        console.log(error);
        brandForm.removeAttribute("disabled");
      }
    });
  }
</script>
