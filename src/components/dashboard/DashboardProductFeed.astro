---
import { getAllProducts } from "@/db/dashboard/allProducts";
import DashboardProductCard from "@/components/dashboard/DashboardProductCard.astro";

const products = await getAllProducts(1);
---

<section>
    <div class="autoGrid">
        {products.map((product) => <DashboardProductCard product={product} />)}
    </div>
</section>
